# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage

# misc
.DS_Store
*.pem
.vscode/

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — NEVER commit secrets. The Slack user token lives only in GitHub Actions Secrets.
.env
.env.*
!.env.example

# typescript
*.tsbuildinfo

# NOTE: state/ is intentionally NOT ignored — the dedupe markers are committed by the workflow.
