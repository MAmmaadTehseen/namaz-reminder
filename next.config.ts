import type { NextConfig } from "next";

/**
 * The Next.js app here is OPTIONAL — only a status dashboard.
 * The reminder engine runs as a plain script (`npm run tick`) on GitHub Actions
 * and never needs `next build`. See README.
 */
const nextConfig: NextConfig = {};

export default nextConfig;
